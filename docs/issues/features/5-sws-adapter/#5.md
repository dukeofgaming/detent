---
type: issue
issue:
    sections:
        - tasks: 4197250954
    lastSyncedAt: "2020-01-01T00:00:00Z"
---

# 5 - Serverless Workflow (SWS) Standard Adapter
<!-- GitHub Issue #5 -->

## Description

Detent currently supports BPMN 2.0 as its first standard adapter (via `transpiler/bpmn/`). ADR-7 explicitly reserves a future SWS adapter slot and establishes the architectural boundary for multi-standard support. Issue #4 confirms SWS is the next standard to integrate.

The **Serverless Workflow** specification is a CNCF sandbox project at v1.0.3 that defines a JSON/YAML DSL for workflow definitions. An official Rust SDK exists at `github.com/serverlessworkflow/sdk-rust` (v1.0.0-alpha6.3) with three crates: `serverless-workflow-core` (DSL models), `serverless-workflow-builders` (programmatic construction), `serverless-workflow-io` (read/write JSON/YAML).

### What needs to be built

A new SWS adapter at `transpiler/sws/` that mirrors the BPMN adapter structure:

1. **Parser** — Read SWS JSON/YAML into the SDK's core types (or handcrafted types if the SDK is insufficient)
2. **Normalizer** — Convert SWS types into the domain IR (`Workflow`, `Graph`, etc.)
3. **Serializer** — Convert domain IR back into SWS JSON/YAML
4. **Validation** — Integrate SWS schema validation into the `validate` command pipeline
5. **Compilation** — Wire SWS parsing into `compile` so MDX can import SWS workflows

### Constraints

- Follow the same Clean Architecture pattern as `transpiler/bpmn/`: parse → normalize → domain IR
- The SWS Rust SDK is pre-1.0 (alpha); decisions need to be made about depending on it vs. handcrafting types
- `serde_json` and `serde_yaml` are already workspace dependencies, so serialization plumbing is available
- SWS uses a fundamentally different workflow model (action-based, event-driven) vs. BPMN (flow-chart with gateways), so the normalizer must handle semantic translation carefully

### Out of scope (for this issue)

- Full event-driven execution of an SWS workflow (the engine doesn't support that yet)
- SWS function/event/service binding execution
- Sub-workflow / workflow composition

## Tasks
<!-- GitHub Section:Tasks:5  -->

- [x] Phase 0 — Research and decision
    - [x] Evaluate `serverless-workflow-core` crate: type coverage, serde support, schema completeness for v1.0.3
    - [x] Decide: depend on the SDK crate vs. handcraft a minimal subset of SWS types — **use SDK crate**
    - [x] Decide: feature-gate SWS support behind `cfg(feature = "sws")` like `xsd-validation` — **no feature gate; always included**
    - [x] Map SWS concepts to domain IR concepts (see Notes below)
- [ ] Phase 0b — Introduce standard-neutral domain type in `detent-core`
    - [ ] Add `lib/core/src/workflow.rs` with `workflow::Process`, `workflow::Node`, `workflow::Edge`, `workflow::NodeKind`
    - [ ] Update `process_graph::Graph` to accept `workflow::Process` instead of `bpmn::Process`
    - [ ] Update `src/transpiler/bpmn/to_domain.rs` to produce `workflow::Process`
    - [ ] Update all affected tests (TDD: failing tests first)
- [ ] Phase 1 — SWS parser and types (`src/transpiler/sws/`)
    - [ ] Create `src/transpiler/sws.rs` module root (ADR-6: `folder.rs` convention)
    - [ ] Create `src/transpiler/sws/types.rs` re-exporting SDK types + SWS-specific constants
    - [ ] Create `src/transpiler/sws/parse.rs`: `parse_sws_json(input: &str)` and `parse_sws_yaml(input: &str)`
    - [ ] Create `src/transpiler/sws/serialize.rs`: `serialize_sws_json` and `serialize_sws_yaml`
    - [ ] Add `serverless-workflow-core = "1.0.0-alpha6.3"` to workspace `Cargo.toml`
    - [ ] Add SWS schema validation (JSON Schema-based, analogous to BPMN XSD validation)
- [ ] Phase 2 — Normalizer (SWS → Domain IR)
    - [ ] Create `src/transpiler/sws/to_domain.rs`: `to_domain_sws_process(wf: &WorkflowDefinition) -> workflow::Process`
    - [ ] Map SWS `State` variants to `workflow::NodeKind`
    - [ ] Map `Transition`/`End` to `workflow::Edge`
    - [ ] Synthesize start/end nodes from `start.stateName` and `end: true`
    - [ ] Map `SwitchState` → `ExclusiveGateway`, `ParallelState` → `ParallelGateway`
    - [ ] Error handling: return `AdapterError` for unmappable or invalid SWS constructs
- [ ] Phase 3 — Serializer (Domain IR → SWS)
    - [ ] Implement `to_sws_workflow(process: &workflow::Process) -> WorkflowDefinition` in `to_domain.rs`
    - [ ] Round-trip test: parse → normalize → serialize → parse → normalize, assert IR equivalence
- [ ] Phase 4 — Integration
    - [ ] Wire SWS import into `transpiler::import` (SWS → MDX, `sws:` type prefix in frontmatter)
    - [ ] Wire SWS compile into `transpiler::compile` (MDX with `sws:` types → SWS JSON/YAML)
    - [ ] Wire SWS validation into `commands::validate` (`.sw.json` / `.sw.yaml` extension dispatch)
    - [ ] Wire SWS output into `commands::compile`
    - [ ] Wire SWS input into `commands::import`
    - [ ] ~~Integration tests for BPMN → SWS round-trip via domain IR~~ (out of scope: different semantic models)
- [ ] Phase 5 — Documentation
    - [ ] Document SWS import/validate/compile usage in CLI help
    - [ ] Document known SWS feature gaps (e.g., unexecutable event triggers, function bindings)

## Notes
<!-- GitHub Section:Notes:5  -->

### Phase 0 decisions (2026-05-11)

1. **SDK vs. handcraft**: Use `serverless-workflow-core` v1.0.0-alpha6.3. Deps are `serde`, `serde_json`, `serde_yaml` only — all WASM-safe and already in the workspace. 1,460 lines of DSL model coverage. Risk: alpha; mitigated by pinning the version.
2. **Feature gate**: No. SWS support always compiled in. Simpler build, no flag proliferation.
3. **Bidirectional**: Full MDX ↔ SWS (both compile and import directions), mirroring the BPMN adapter.
4. **MDX type prefix**: `sws:` (e.g. `sws:OperationState`, `sws:SwitchState`), mirroring `bpmn:Task`.
5. **Domain IR**: Introduce a new standard-neutral `detent_core::workflow::Process` type (Phase 0b) rather than reusing `bpmn::Process`. The existing BPMN adapter's `to_domain.rs` will be updated to map to `workflow::Process` instead. `Graph` validation then works for both BPMN and SWS without BPMN-named types in the domain layer.
6. **SWS concept map**:

| SWS concept | Domain `workflow::` equivalent |
|---|---|
| `WorkflowDefinition` | `Process` |
| `State` (any variant) | `Node` |
| `OperationState`, `EventState`, `InjectState` | `NodeKind::Task` |
| `SwitchState` (data/event-based) | `NodeKind::ExclusiveGateway` |
| `ParallelState` | `NodeKind::ParallelGateway` |
| `start.stateName` target state | synthesized `NodeKind::Start` entry edge |
| `Transition { nextState }` | `Edge` |
| `End: true` on a state | synthesized `NodeKind::End` exit edge |

### Architecture — Layer assignment (2026-05-07)

1. **2026-05-07**: Confirmed SWS adapter belongs in the **Adapter** layer per ADR-7, mirroring the BPMN adapter.
    1. SWS parsing and schema validation: Adapter/Infrastructure
    2. SWS → domain IR normalization: Adapter (Translator/Mapper pattern)
    3. Domain IR → workflow semantics: Domain (reuses existing `Graph::validate()`, execution rules)
    4. Orchestration (import, validate, compile): Application

### Architecture — SDK dependency risk (2026-05-07)

1. **2026-05-07**: The `serverless-workflow-sdk-rust` is at v1.0.0-alpha6.3. Key evaluation criteria:
    1. Are the core model types complete for the v1.0.3 spec?
    2. Do they serialize/deserialize correctly with serde?
    3. Is the crate actively maintained (last release Jan 2025)?
    4. Does the IO crate handle both JSON and YAML?
    - If the SDK is insufficient, handcraft a minimal subset of types in `transpiler/sws/types/`.

### Architecture — BPMN vs SWS semantic gap (2026-05-07)

1. **2026-05-07**: SWS and BPMN have fundamentally different workflow models:
    1. BPMN is flow-chart oriented: `sequenceFlow` connects `startEvent` → `task` → `gateway` → `endEvent`
    2. SWS is action/state oriented: a `State` has `actions[]` and `transition`/`end`, with event-driven, parallel, and callback state types
    3. The normalizer must translate SWS states into the graph IR without losing semantic information
    4. Some SWS concepts (event triggers, function definitions, retry policies) have no BPMN equivalent — these may need to be stored as extensions on the domain IR

### References

- ADR-7: Clean Architecture boundaries (SWS as future adapter)
- Issue #4: Graph validation (confirms SWS as next standard)
- SWS specification v1.0.3: https://github.com/serverlessworkflow/specification
- Rust SDK: https://github.com/serverlessworkflow/sdk-rust

<!-- GitHub Title:5 5 - Serverless Workflow (SWS) Standard Adapter -->
<!-- GitHub Title:5 5 - Serverless Workflow (SWS) Standard Adapter -->
