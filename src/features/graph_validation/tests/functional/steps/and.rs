// `And` step definitions for the graph_validation functional level.
//
// In Gherkin, `And` continues the preceding Given/When/Then, so an `And` line
// is matched by a Given/When/Then step definition. This module exists as the
// home for any step whose canonical phrasing reads as an `And` continuation
// shared across functional scenarios (per ADR-8).
