use cucumber::{given, then};

use super::GraphValidationWorld;

#[given("graph validation has no dedicated CLI command yet")]
fn given_no_cli(_world: &mut GraphValidationWorld) {}

#[then("this placeholder scenario is skipped")]
fn then_placeholder(_world: &mut GraphValidationWorld) {}
