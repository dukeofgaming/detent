// `When` step definitions for the graph_validation unit level.
//
// Unit scenarios are self-contained in `scenarios/`. This module exists as
// the home for any `When` step that becomes shared across unit scenarios.
