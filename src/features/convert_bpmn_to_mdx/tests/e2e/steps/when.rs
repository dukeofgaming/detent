// `When` step definitions for the convert_bpmn_to_mdx e2e level.
//
// E2E scenarios are self-contained in `scenarios/`. This module exists as
// the home for any `When` step that becomes shared across e2e scenarios.
